version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.16.1
    working_directory: ~/repo
    steps:
      - checkout

      - run: sudo npm i -g pnpm@5.4.6
      - run: pnpm i
      - run: pnpm run lint
      - run: pnpm run build -r
      - run: pnpm t
      - run: pnpm run coverage

      - persist_to_workspace:
          root: .
          paths:
            - packages/*/dist/*

  publish:
    docker:
      - image: circleci/node:12.16.1
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
      - run: sudo npm i -g pnpm@5.4.6
      - run: pnpm i --frozen-lockfile -P
      - run: pnpm -r publish --access public --no-git-checks

workflows:
  version: 2
  package:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/
      - publish:
          context: iyegoroff
          requires:
            - build
          filters:
            tags:
              only: /^v.*/
            branches:
              ignore: /.*/
